import { StyleSheet, Text, View } from "react-native";
import React from "react";
import App from "../../components/pushNotifications";

export const Statistics = () => {
  return (
    <View>
      <App />
    </View>
  );
};

const styles = StyleSheet.create({});
